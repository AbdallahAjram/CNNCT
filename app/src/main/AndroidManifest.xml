<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CNNCT"

        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".homepage.view.HomeActivity"
            android:exported="false" />
        <activity android:name=".auth.controller.LoginActivity"
          android:exported="false"/>
        <activity android:name=".auth.controller.SignupActivity" android:exported="false"/>
        <activity android:name=".auth.controller.CompleteProfileActivity" android:exported="false"/>
        <activity android:name=".groups.view.GroupActivity" android:exported="false"/>
        <activity
            android:name=".chat.view.ChatActivity"
            android:exported="false" />
        <activity android:name=".settings.view.SettingsActivity" android:exported="false"/>
        <activity android:name=".calls.view.CallsActivity" android:exported="false"/>
    </application>

</manifest>